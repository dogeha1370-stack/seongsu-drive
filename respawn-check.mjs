import fs from 'node:fs';import ts from 'typescript';import assert from 'node:assert/strict';const code=ts.transpileModule(fs.readFileSync('app/pedestrians.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.ES2022,target:ts.ScriptTarget.ES2022}}).outputText;const {person,damagePerson,respawnStep}=await import('data:text/javascript;base64,'+Buffer.from(code).toString('base64'));const p=person(0,0,0);assert.equal(respawnStep(p,20,()=>true),false);damagePerson(p,100);assert.equal(respawnStep(p,9.99,()=>true),false);assert.equal(p.hp,0);assert.equal(respawnStep(p,.01,()=>false),false);assert.equal(p.hp,0);assert(respawnStep(p,0,()=>true,()=>.75));assert.equal(p.hp,100);assert.equal(p.aggro,0);assert.equal(p.vx,0);assert.equal(p.lean,0);assert.notEqual(p.x,0);assert.equal(p.homeZ,p.z);assert.equal(p.respawnIn,0);console.log('Respawn checks passed: 10 second delay, safe placement retry, full health and state reset.');
